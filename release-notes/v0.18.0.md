# 985gmgn助手 v0.18.0

## 本版重点

v0.18.0 新增"持仓暴涨提醒"：你持有的币短时间急涨时，画面正中直接弹 🚀 大卡片，不用一直盯着持仓面板。装完不需要填任何东西，也不需要授权——行情走 GMGN 的公开接口，全程不接触任何登录凭据。

## 主要更新

- 新增"持仓暴涨提醒"开关（默认开启），阈值默认「5 分钟涨幅 ≥ 20%」，配置页可选 +10% / +20% / +30% / +50% / +100%。
- 触发时在画面正中弹出 🚀 绿色大卡片：代币名、5 分钟涨幅、当前价格，点击直达代币页；15 秒自动消失、悬停暂停、× 可关闭、最多叠 3 张。
- **持仓清单自动收集**：打开持仓面板时，插件读取该面板列出的持仓（含所属链）并缓存到本地，**之后关掉面板也会继续监控**。
- **多链同时监控**：GMGN 的持仓面板只显示当前链，切换到别的链再打开一次面板，那条链的持仓也会进入清单；之后无论你停在哪条链，所有已收录链的持仓都在后台一起盯。
- 行情查询走 GMGN 公开接口，请求显式声明不带凭据（`credentials: omit`）；30 秒一轮、按链分批；同一代币 10 分钟内不重复提醒；标签页切到后台时暂停轮询。

## 安装方法

### 新用户

1. 下载 `985gmgn-helper-setup-v0.18.0.exe`。
2. 可用同名 `.sha256` 文件核对安装器摘要。
3. 运行安装器并点击"安装 / 修复"。
4. 在 Chrome `chrome://extensions/` 或 Edge `edge://extensions/` 开启开发者模式。
5. 点击"加载已解压的扩展程序"，选择 `%LOCALAPPDATA%\985gmgn-helper\Extension`。
6. 打开 [GMGN BSC](https://gmgn.ai/?chain=bsc)，点击插件图标完成配置。

### 已安装 v0.8.0 及以上的用户

无需重新运行安装器。打开插件配置页后应看到"发现 v0.18.0"，点击"一键升级"即可。

安装器暂未购买商业代码签名证书，Windows SmartScreen 可能显示"未知发布者"。请从本仓库 Release 下载，并按 `.sha256` 文件核验摘要。

## 使用说明

### 持仓暴涨提醒

装完即用，无需任何配置。第一次打开 GMGN 底部的"持仓"面板时，插件会记下你当前链的持仓；之后面板关掉也会继续在后台盯着。想让别的链的持仓也纳入，切到那条链再打开一次持仓面板即可。持仓短时急涨超过阈值时，画面正中会弹出 🚀 卡片，点击进入该代币页面。阈值和开关都在插件配置页。

### 价格提醒醒目化

GMGN 原生的到价/市值提醒（你自己设的价格提醒）触发时，改为画面正中的大卡片，涨绿跌红。

### 特别关注

追踪事件卡动作文案右边、钱包列表行名字后、地址页关注按钮旁都可打 ☆。已关注钱包 ★ 旁的色块打开调色浮层：7 预设色 / 炫彩 / 自定义取色 + 置顶开关。面板头部 ★N 打开管理面板统一增删改。

### 新宣言弹窗与宣言标签页

新宣言在画面正中弹出官方悬浮卡样式的大卡片，点击直达代币页；喊单窗"宣言"标签按时间倒序浏览当前宣言。

### Dev 战绩条

战绩条格式 `迁X发Y·Z%·峰$MC`，峰值按档位着色（灰 <$100K、蓝 $100K–1M、紫 $1M–10M、金 ≥$10M）。

### 喊单黑名单与隐藏闪电交易

喊单卡片屏蔽按钮 + 面板黑名单管理；"隐藏闪电交易按钮"开关默认开启，可隐藏 frontrun 插件的闪电交易按钮。

### 功能开关

配置页可分别控制重点 Dev 高亮、Dev 战绩常显、重点 Dev 悬停详情、详情页一键收藏、喊单黑名单、新宣言弹窗、宣言列表标签、特别关注高亮、持仓暴涨提醒、价格提醒醒目化和隐藏闪电交易按钮。

## 更新方式

插件在浏览器启动时及每 6 小时检查 GitHub 最新 Release。发现更新后：

1. 插件图标显示绿色 `UP`。
2. 打开插件配置页查看当前版本和最新版本。
3. 可先点击 GitHub 链接阅读完整版本说明。
4. 点击"一键升级"，等待下载、校验、备份和替换完成。
5. 插件自动重载后，配置页显示新版本号。

如果本地更新器不可用，插件会提供 GitHub 最新版本入口，不会静默失败。

## 安全与隐私

- 更新器只访问本仓库 GitHub Release API 和下载地址。
- 下载包必须通过 SHA256、文件白名单、manifest 名称、版本和固定扩展 ID 校验。
- 更新前保留旧版备份，替换失败自动恢复；仅保留最近 3 份备份。
- 持仓暴涨提醒**不读取、不存储、不发送任何登录凭据**：持仓清单来自页面上已渲染的持仓面板，行情查询走 GMGN 公开接口且显式不带凭据。
- 持仓清单（代币地址与代号）仅保存在浏览器本地存储中，不上传任何服务器。
- 特别关注名单、颜色与置顶设置、已见宣言同样仅保存在本地。
- 插件不读取私钥、助记词、密码或 API Key。
- 本工具为第三方开源插件，并非 GMGN 官方产品。

## 兼容范围

- Windows 10/11。
- Chrome 111+、Microsoft Edge 或兼容 Chromium 浏览器。
- GMGN 各链战壕、代币页、地址页、喊单面板、钱包追踪面板及持仓面板。

[查看源码](https://github.com/0xuezhang985/985gmgn-helper/tree/v0.18.0) · [提交问题](https://github.com/0xuezhang985/985gmgn-helper/issues)

# better gmgn v0.38.0

## fomo 混排卡：外观与 GMGN 原生卡对齐

- 两行结构、同高、透明底：行1 头像 + 名字 + 动作（买入/卖出/换仓/观点，按方向着色）+ 时间；行2 金额 + 代币 logo + 币名 + MC。来源只用一个 [fomo] 小标区分，不再有彩色边框和底色。
- 观点正文改单行内嵌，悬停看全文。

## fomo 浮窗支持 Robinhood 链

`/robinhood/token/…` 代币页现在会出现 fomo 入口，持仓者/观点面板照常可用（fomo 数据源本就覆盖 Robinhood 链，networkId 4663）。顺带支持 monad。fomo 不覆盖的链依旧不显示入口。

## 其它

混排卡点击与当前链识别扩到 GMGN 全部链段，Robinhood 链的混排卡点击同样走站内无刷新跳转。

刷新扩展（chrome://extensions → ⟳）生效。

# better gmgn v0.40.1

## 修复：标注人物持仓徽章（👤N）失效

代币旁的 👤N 徽章（有几个被标注的人持有它，悬停看是谁）此前一直不出现。排查：GMGN 的持仓接口现在要求带其前端的客户端参数，裸请求被 Cloudflare 403；且旧实现还把登录 cookie 剥掉了。本版从页面自己发过的请求里抄参数、带登录态访问，徽章恢复工作。

前提：浏览器已登录 GMGN（接口需要登录态，你日常使用即满足）。

刷新扩展（chrome://extensions → ⟳）生效。

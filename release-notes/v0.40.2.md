# better gmgn v0.40.2

## 标注人物持仓徽章：请求量加固

徽章的请求模型：请求数 = 名单人数（默认 3 人），每链 2 分钟一轮缓存，之后所有卡片上的徽章渲染都是本地查表、零网络——和追踪流里滚过多少张卡完全无关，约 1.5 次请求/分钟，远低于 GMGN 页面自身流量。

本版补两处防放大：

- 持仓缓存按链各自保存，bsc 和 sol 之间来回切页不再触发重拉。
- 名单里人与人的请求之间垫 250ms 最小间隔，就算自定义名单加到几十人也是平滑串行，不会突发。

刷新扩展（chrome://extensions → ⟳）生效。

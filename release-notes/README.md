# Release Notes 维护规则

每次创建 `vX.Y.Z` 标签前，必须先新增同名 `release-notes/vX.Y.Z.md`。

版本说明至少包含以下部分：

- `## 本版重点`
- `## 安装方法`
- `## 使用说明`
- `## 更新方式`
- `## 安全与隐私`

标签发布工作流会校验文件存在、章节完整且内容长度足够；不符合要求时直接停止发布。推送到 `main` 后，同步工作流会把版本说明更新到对应的现有 GitHub Release 页面。

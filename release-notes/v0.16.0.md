# 985gmgn助手 v0.16.0

## 本版重点

v0.16.0 把"特别关注"升级成完整的工作台：钱包追踪面板头部新增 ★N 管理入口统一查看和设置全部特别关注；地址页关注钱包时可顺手打星；颜色支持自定义取色和炫彩流动高亮；还可以给重点钱包开"置顶推送"——它的新动作会在追踪面板顶部置顶 10 秒。

## 主要更新

### 特别关注管理面板

- 钱包追踪面板头部（标签行区域）新增 **★N** 按钮，点击展开管理面板：列出全部特别关注钱包（颜色、置顶状态、备注、地址）。
- 每行可直接：点色块改颜色/置顶、点 📌 切换置顶、点"移除"取消关注。
- 顶部输入框支持手动添加：粘贴钱包地址 + 备注，一键加入特别关注。

### 添加钱包时同步打星

- 钱包地址页的"追踪/已追踪"按钮旁常显 ☆：关注一个新钱包的同时点一下即可加入特别关注（备注自动取钱包名）。

### 颜色升级：自定义 + 炫彩

- 调色浮层在 7 个预设色之外新增：**炫彩**（卡片背景、左边条、★ 彩虹流动动画）和**自定义取色器**（任意颜色）。

### 置顶推送

- 调色浮层和管理面板都有"📌 新推送置顶 10 秒"开关。
- 开启后，该钱包在追踪流出现新事件时，卡片副本自动置顶在面板顶部 10 秒：金色描边、📌 角标、最多同时 3 张，点击直达代币页，也可 × 手动关闭。

## 安装方法

### 新用户

1. 下载 `985gmgn-helper-setup-v0.16.0.exe`。
2. 可用同名 `.sha256` 文件核对安装器摘要。
3. 运行安装器并点击"安装 / 修复"。
4. 在 Chrome `chrome://extensions/` 或 Edge `edge://extensions/` 开启开发者模式。
5. 点击"加载已解压的扩展程序"，选择 `%LOCALAPPDATA%\985gmgn-helper\Extension`。
6. 打开 [GMGN BSC](https://gmgn.ai/?chain=bsc)，点击插件图标完成配置。

### 已安装 v0.8.0 及以上的用户

无需重新运行安装器。打开插件配置页后应看到"发现 v0.16.0"，点击"一键升级"即可。

安装器暂未购买商业代码签名证书，Windows SmartScreen 可能显示"未知发布者"。请从本仓库 Release 下载，并按 `.sha256` 文件核验摘要。

## 使用说明

### 特别关注

追踪事件卡动作文案右边、钱包列表行名字后、地址页关注按钮旁都可打 ☆。已关注钱包 ★ 旁的色块打开调色浮层：7 预设色 / 炫彩 / 自定义取色 + 置顶开关。面板头部 ★N 打开管理面板统一增删改。

### 新宣言弹窗与宣言标签页

新宣言在画面正中弹出官方悬浮卡样式的大卡片，点击直达代币页；喊单窗"宣言"标签按时间倒序浏览当前宣言。

### Dev 战绩条

战绩条格式 `迁X发Y·Z%·峰$MC`，峰值按档位着色（灰 <$100K、蓝 $100K–1M、紫 $1M–10M、金 ≥$10M）。

### 喊单黑名单与隐藏闪电交易

喊单卡片屏蔽按钮 + 面板黑名单管理；"隐藏闪电交易按钮"开关默认开启，可隐藏 frontrun 插件的闪电交易按钮。

### 功能开关

配置页可分别控制重点 Dev 高亮、Dev 战绩常显、重点 Dev 悬停详情、详情页一键收藏、喊单黑名单、新宣言弹窗、宣言列表标签、特别关注高亮和隐藏闪电交易按钮。

## 更新方式

插件在浏览器启动时及每 6 小时检查 GitHub 最新 Release。发现更新后：

1. 插件图标显示绿色 `UP`。
2. 打开插件配置页查看当前版本和最新版本。
3. 可先点击 GitHub 链接阅读完整版本说明。
4. 点击"一键升级"，等待下载、校验、备份和替换完成。
5. 插件自动重载后，配置页显示新版本号。

如果本地更新器不可用，插件会提供 GitHub 最新版本入口，不会静默失败。

## 安全与隐私

- 更新器只访问本仓库 GitHub Release API 和下载地址。
- 下载包必须通过 SHA256、文件白名单、manifest 名称、版本和固定扩展 ID 校验。
- 更新前保留旧版备份，替换失败自动恢复；仅保留最近 3 份备份。
- 特别关注名单、颜色与置顶设置仅保存在浏览器本地存储中，不上传任何服务器。
- 置顶推送只是页面内复制展示已有卡片，不发起任何额外网络请求。
- 插件页面功能只请求 GMGN 站内接口，同源自动带会话，不使用第三方 API Key。
- 插件不读取私钥、助记词、密码或 API Key。
- 本工具为第三方开源插件，并非 GMGN 官方产品。

## 兼容范围

- Windows 10/11。
- Chrome 111+、Microsoft Edge 或兼容 Chromium 浏览器。
- GMGN BSC 战壕、代币页、地址页、喊单面板及钱包追踪面板。

[查看源码](https://github.com/0xuezhang985/985gmgn-helper/tree/v0.16.0) · [提交问题](https://github.com/0xuezhang985/985gmgn-helper/issues)

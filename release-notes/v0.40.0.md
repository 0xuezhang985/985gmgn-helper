# better gmgn v0.40.0

## fomo 推送：秒级实时

985monitor 服务端把 fomo 事件接进了 SSE 推送通道（已检查确认全链就绪），插件本版跟进：后台常驻订阅实时流，新事件到达即刻通知页面上屏——**端到端延迟从 ~14 秒降到秒级**（采集落盘节流 5 秒 + 推送毫秒级）。

- 只消费 fomo 帧；坏帧跳过不断流，断线自动退避重连。
- 原 15/18 秒轮询保留为兜底：SSE 断档期间照常有数据，只是回到轮询节奏。

刷新扩展（chrome://extensions → ⟳）生效。

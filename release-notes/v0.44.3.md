# better gmgn v0.44.3

## fomo 持仓占比支持 ETH 和 Base

之前只有 BSC 的币能显示"Fomo 持仓占比"，其它链一片空白——因为算占比要用代币总供应量，而取供应量的代码写死了只认 BSC。现在 ETH 和 Base 的币也能正常显示占比。

Solana 和 Robinhood 暂时还不支持（公共节点取不到供应量，需要带 key 的服务），这两条链上占比仍为空，其余数据不受影响。

刷新扩展（chrome://extensions → ⟳）生效。

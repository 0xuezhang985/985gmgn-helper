# better gmgn v0.39.2

## fomo 推送提速

排查延迟构成：985 采集端是实时 WS（5 秒落盘节流，观点也走实时流），大头在插件自己的 40~45 秒轮询间隔。本版把后台拉取控频压到 15 秒、前台触发 18 秒——数据未变更时是 ETag 304 条件请求（只有响应头），带宽成本可忽略。

端到端延迟：平均 ~27 秒 → **~14 秒**，最坏 ~50 秒 → **~23 秒**。

刷新扩展（chrome://extensions → ⟳）生效。

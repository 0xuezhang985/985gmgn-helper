# Changelog

## 0.45.8 - 2026-08-28

- **修复跨链加钱包报 `HTTP 401`**。两处都错了，实测确认：
  - **鉴权**：这个接口只认 `Authorization: Bearer`，光带 cookie 一律 `401 code 40101611 "empty token"`。令牌就是 GMGN 自己放在本站 `localStorage.tgInfo` 里的 `token.access_token`（只在本机读、只发回 gmgn.ai）。顺带纠正一条旧结论：GMGN 的鉴权请求走的是 **XHR 而非 fetch**，也不在 Web Worker 里——之前"主线程钩子抓不到鉴权头"的判断是错的。
  - **请求体字段名全是猜的**：以前发 `{chain, address, name}`，服务端回「`FollowWalletsRequest.WalletAddresses` required」。正确形状取自 GMGN 自己的分包：`wallet_addresses` 是**数组**，`remark_addresses` 是 `[地址, 备注, emoji]` **三元组数组**。补上鉴权后换成正确字段名，服务端才开始正常校验地址本身。
- **修复 Solana 钱包的「特别关注」完全失效**。三处写死了 EVM：
  - 地址规范化统一 `toLowerCase()`——而 **Solana 是大小写敏感的 base58**，转小写等于换了个地址，存进去也永远匹配不上。
  - 选择器写死 `a[href*="/address/0x"]`，SOL 的地址链接根本不匹配 → 钱包行、追踪卡、地址详情页全都挂不上星标。
  - 添加时的校验写死 `/^0x[a-f0-9]{40}$/`，SOL 地址直接被拒。
  - 现在钱包地址走独立的链感知规范化：EVM 照旧转小写，Solana 原样保留；通用的 `normalizeAddress` 不动（它还被代币地址等处共用，改了会和已存数据对不上）。喊单功能里同类的两处写死一并放宽。

## 0.45.7 - 2026-08-28

- **持仓暴涨提醒改按「你的实际成本」算**（修复「刚买完立刻误报一次」）。
  - 旧逻辑拿 GMGN 的 `price_5m`（5 分钟前的价格）当基准。而你追高买进去的那一刻，**那 5 分钟涨幅正是你追的那一波**——买完第一轮查价就直接报「5分钟 +50%」，可你其实一分钱没赚。
  - 现在基准换成这一仓的**每币平均成本**：`page-bridge` 从持仓行的 React props 里读 `avg_cost`（没有就用 `history_avg_cost`，再没有就 `总成本 ÷ 持仓数量`）——字段名全部取自 GMGN 自己分包里的持仓对象，不是猜的。卡片上也从「5分钟」改成「**较成本**」。
  - 成本实在读不到（面板没展开等）就退回「第一次看到这个仓位时的价格」当基准，同样不会一买就报。
  - **首轮只建基准不弹**：打开页面时老仓位可能已经 +300%，不该一上来就报一串；并且改成**每越过一个新档位才报一次**（涨幅 ÷ 阈值 取整），不再是「只要还在阈值以上就按冷却反复报」。
- **顺带修掉另一条误报路径**：卖掉某条链上**最后一个**持仓后，对账逻辑因为「本轮没看到任何行」而整块跳过，那个币会永远赖在监控清单里继续查价、继续报警。现在「面板开着但一行都没有」也会正常对账；面板压根没渲染时仍然绝不误删。
  - 这条是修测试夹具时暴露出来的——`verify-surge.js` 伪造的持仓行缺少余额类字段，一直没被识别成持仓，导致这条路径从来没被真正跑到过。夹具已修，该套 10 项现在全过。

## 0.45.6 - 2026-08-27

- **修复「关掉别的功能后 Flap 徽章就不显示了」**。用户把「特别关注高亮」关掉之后，Flap 税收徽章跟着消失——**而它自己的开关明明还开着**。
  - 病根是嵌套错位：`scanFlapBadges()` 和 `scanMarkedBadges()` 被写在 `scanSpecialWallets()` 的函数体末尾，而那个函数开头有一句「`enableSpecialWallet === false` 就 `return`」。于是一关特别关注，这两个毫不相干的功能就一起哑掉了。
  - **「标注人物持仓徽章」是同一个 bug 的受害者**，一样是开着却不工作，这次一并修好。
  - 两个函数本来就各自带独立开关守卫，现在直接挂到主扫描循环上，各自兜住异常，互不牵连。顺带在性能诊断里各占一项（`data-gdh-perf` 里的 `marked` / `flap`），以后谁慢一眼看得出。
  - 把所有带开关的功能过了一遍，确认没有别的功能被套在无关开关底下。

## 0.45.5 - 2026-08-27

- **修复「flap 徽章开了却不显示」**。真机上抓到的：38 张 Flap 战壕卡里只有 18 张出了徽章，等 15 秒也只多一张——**是停住了，不是还在加载**。把没出徽章的那几个币拿去直接打链上，`getPoolStateData` 返回和有徽章的一模一样，说明它们都是正经 Flap 币、合约调用本身没问题。
  - 病根在内容脚本这层缓存：`flapInfoCache.set(token, res || { ok:false })` **没有 TTL 也没有重试**。任何一次瞬时失败——节点抖动、service worker 正好被挂起导致 `sendMessage` 返回 undefined、网络闪断——都会把这个币**永久钉死**，此后每轮扫描都直接跳过，徽章直到整页刷新都不会再出现。后台那层缓存只有 60 秒 TTL、本来乐意重试，但内容脚本再也不问了。
  - 后台其实早就区分了 `not-flap`（这币根本不是 Flap 系，地址尾号 7777/8888 只是撞巧，是定论）和 `rpc-failed`（节点抖动），内容脚本却把两种一视同仁全部钉死。
  - 现在：`not-flap` 仍然只问一次；其它失败按**指数退避重试**（8 秒起步，最多 5 次），既能自愈也不会拿一堆失败的币去连打节点。
  - 顺带留了诊断：取不到时把原因写在卡片的 `data-gdh-flap-fail` 上，再有人报「不显示」，一眼能分清是节点没取到还是压根不是 Flap 币。

## 0.45.4 - 2026-08-27

- **修复「折叠着的 fomo 面板，登录态过期后没法续、点不开」**。折叠态的样式把整个列表 `display:none` 收起来了，而登录过期的引导（含「打开 fomo 并登录」和「重试」两个按钮）恰恰就渲染在这个列表里——于是折叠时一过期，面板就变成一条什么都没有的空白横条：既看不到出了什么事，也够不到修复的入口。现在出错时给面板加一个标记，让样式把列表放出来；**不动你存的折叠偏好**，数据一恢复正常就自动收回去。

## 0.45.3 - 2026-08-27

- **再修 fomo 登录态"还是会掉"**。这回是在真实浏览器里量出来的，不是推断：
  - **令牌寿命是整 60 分钟**（`iat`→`exp` 恰好 3600 秒）。上一版按"约 70 分钟"设阈值，是错的。
  - **病根：两个续期者在抢同一条链**。privy 每次续期都会作废旧的 refresh_token，而插件把新令牌写回页面 `localStorage` **并不会**更新页面 SDK 内存里的那一份——同一个 document 内的写入不触发 `storage` 事件，SDK 根本感知不到。于是页面迟早拿着已作废的旧 refresh 去续，撞上 privy 的**复用检测**，整条会话连坐作废，表现就是"又要重新登录"。
  - **改成同一时刻只有一个续期者**：只要 fomo 页面还活着，插件就完全让位。实测 privy SDK 会在过期那一刻准时续期，**隐藏标签页也照续**，所以不分前后台。插件只做镜像。
  - **页面心跳判断在场与否**：content script 每 15 秒上报一次存活。`tabs.query` 只能看出标签页在不在，看不出里面的 JS 还跑不跑——Chrome 会冻结长期后台标签页，冻住的页面不会续期。心跳断了 45 秒才判定页面已退场，由插件接管。
  - **接管后刷新残留页面**：能走到"插件自己轮换"这一步却还有 fomo 标签页在，说明那页被冻结了。它内存里的 refresh 刚被作废，等用户回头点开就会撞枪口——先刷一下让 SDK 重启读到新链。
  - **过期撞车时先等页面**：接口按需续期正好撞上过期那几秒时，先等最多 6 秒看页面有没有续出新令牌，等到就直接采纳，绝不自己轮换。
  - **镜像窗口 20 秒 → 5 秒**：页面一续期，插件手里的 refresh 立刻就是废的，镜像慢一秒就多一秒踩空的窗口。
  - **续期阈值 35 → 20 分钟**：续得越勤、轮换越多，分叉机会越多。60 分钟寿命下留 20 分钟余量，闹钟 5 分钟一响，过期前还有四次机会。
  - **开机补一次**：浏览器关着的时间不跑任何代码，`onStartup` 时先强制查一次。
  - **留了续期流水**：最近 20 条（成功／失败／让位／接管／采纳页面令牌）存在本地，下次出问题能查，不用再靠猜。

## 0.45.2 - 2026-08-27

- **换个办法保住 fomo 登录态**（此前反复"很快过期"）。翻出两个真问题：
  - **保活闹钟形同虚设**：`chrome.alarms.create` 写在模块顶层，而 service worker 每次休眠/唤醒都会重跑整个模块——闹钟被反复重建、计时永远归零，10 分钟的周期实际从没走到过。现在用 `alarms.get` 判存在，已存在就不重建。
  - **续期阈值贴着悬崖**：实测令牌总寿命约 70 分钟（不是整 1 小时），而上版设的是"剩不到 12 分钟才续"——SW 挂起期间不跑代码，醒来往往已经过期。改为剩余不足 35 分钟就续（约走完一半寿命即续），同时保留"还新鲜就不轮换"避免无谓分叉。
  - **不再只靠闹钟**：任何一次页面消息（fomo 拉取、徽章、混排…）都会顺手检查一次要不要续期，带 60 秒防抖。用户在用 GMGN 就一定有消息流，比闹钟可靠得多。
  - **面板先自救再求人**：浮窗遇到"登录态过期"时，先让后台用 refresh 换一张新令牌，成功就自动重载数据；实在续不动才显示重新登录引导。

## 0.45.1 - 2026-08-27

- **修复"很多币算不出持仓占比"**。之前两版一直在修分母（代币总供应量），实测四条链的供应量其实都正常取得到——真正的空缺在**分子**：持仓数量只裸取 fomo 的 `humanAmount` 字段，一旦 fomo 换个字段名或把它包进子对象，这里就是 undefined、分子归零、占比整块显示为 `—`。现在三级兜底：本名 → 深度搜同义字段（amount/balance/quantity/size…）→ 用持仓金额 ÷ 单价反推。持仓者的「链上第几名」徽章同一口径一并修正。
- **修复表格模式下特别关注出现双竖条**：同一行的外层链接与内层内容 DIV 都会被打上特别关注标记，两层各画一条 3px 竖条。现在内层只保留底色，竖条只由最外层画。

## 0.45.0 - 2026-08-27

- **添加钱包支持跨链**：在 EVM 页面粘 Solana 地址（或反过来）时，弹窗里出现一条提示和「直接加到 SOL/BSC」按钮——直接调 GMGN 自己的 follow 接口把钱包加到目标链，**不跳转、不用先切链**。弹窗里勾了「同时特别关注」的话一并登记。EVM 地址在任意 EVM 链页面都不会误报。
- **修复战壕卡在非 BSC 链上完全不工作**：取卡片地址的正则写死了 `/bsc/token/`，在 `/robinhood/token/`、`/base/token/` 等页面上取不到地址 → 整张卡被跳过 → Dev 战绩、创建者数据、重点 Dev 高亮全都不渲染（实测该页 16 张卡的插件数据属性全空）。卡片选择器里的同类写死也一并改为全链通用。
- **持仓暴涨提醒卡点击改为站内无刷新跳转**，和点追踪流里的 fomo 卡一致；顺带把喊单、宣言等处剩余的整页跳转全部统一。
- **fomo 面板新增折叠按钮**（标题栏 ▤）：折叠后只保留「Fomo 持有人数」与「Fomo 持仓占比」两块统计，列表和标签栏收起，状态记忆。
- **fomo 混排卡与原生行左缘对齐**：卡片此前有 4px 内缩，链条色竖条比 GMGN 原生的那条右移了一截，现已贴齐。

## 0.44.4 - 2026-08-27

- **fomo 持仓占比补齐 Solana 与 Robinhood**（0.44.3 只补了 ETH/Base）。之前卡在"必须用各链 RPC 读 totalSupply"的思路上，而 Solana/Robinhood 的公共 RPC 都要付费或带 key。实际上 GMGN 自己的代币接口所有链通用且直接返回 `total_supply`（已是人类可读单位）——实测 Solana CATE 9.64 亿、Robinhood HOOD10 10 亿。
  - 现在取供应量优先走 GMGN 接口（全链覆盖），EVM 链保留直读链上作兜底（接口异常时仍能出数）。
  - 至此 BSC / ETH / Base / Solana / Robinhood 的持仓占比都能正常显示。

## 0.44.3 - 2026-08-27

- **fomo 持仓占比支持 ETH 与 Base**（修"有些链的币不显示持仓占比"）。占比要用代币总供应量换算，而取供应量的实现写死了 `chain !== 'bsc'` 就直接返回 unsupported-chain——只有 BSC 能算出占比，其它链一律空白。现在按链走各自的公共 RPC（端点逐个实测过能读 totalSupply/decimals），供应量缓存也改为按链隔离。
  - 已支持：BSC / ETH / Base。Solana 与 Robinhood 暂不支持（公共 RPC 实测取不到，需要带 key 的节点），这两条链上占比仍显示为空，其余统计与功能不受影响。

## 0.44.2 - 2026-08-27

- **修复"暴涨提醒报了自己没有的持仓"**，两处根因：
  - **清单只加不减**：持仓清单跨会话保留最多 80 个币，卖光/清仓的币一直留着，它日后一涨照样弹提醒。现在做对账——持仓面板本轮渲染过的链，清单里在该链上没出现的币即视为已清仓，自动移除（面板没打开时绝不误删）。弹提醒前再确认一次该币仍在清单里。
  - **误收非持仓代币**：0.43.0 给持仓行加的 fiber 兜底会全站扫 `a[href*="/token/"]`，战壕卡、搜索结果只要能读出 `{chain,address,symbol}` 就被当成持仓收进清单。现在兜底限定在持仓表格内（锚点取自真实页面实测：持仓行的 `data-sentry-source-file` 是 `PositionTableColumns*.tsx`）。
- **修复 page-bridge 每轮扫描崩溃**（0.44.1 引入）：rAF 调度的变量声明在 `scanCards` 之后，命中 TDZ 导致 `scanCards` 每次都从第一行抛错——持仓行、追踪卡的数据标记全部失效，连带暴涨提醒、👤 徽章、fomo 混排锚定一起哑掉。声明已上移。

## 0.44.1 - 2026-08-27

- **性能大修（修"整个 GMGN 页面都卡"）**，三处实打实的性能债：
  - **断掉自触发循环**：插件自己的节点每秒都在小改（fomo 卡时间文本等），这些变动此前会触发 MutationObserver → 全量扫描——等于自己驱动自己每秒跑一遍全部扫描器。现在插件自身节点内的变动一律不触发扫描。
  - **自适应节流**：全量扫描按单轮耗时的指数均值自动拉大间隔（>25ms → 2s，>50ms → 3s，恢复即回落）；徽章最多晚两三秒出现，换整页不卡。诊断口：`document.documentElement` 的 `data-gdh-perf` 属性可直接读单轮耗时。
  - **Flap 战壕扫描去大头**：每张战壕卡每秒正则遍历全部后代找原生税标——几百张卡就是每秒上万节点。徽章行已建好的卡直接跳过。
  - 顺带：fomo 卡秒级时间改 5 秒粒度，减少无意义的 DOM 写入。
- **👤 徽章位置按要求改到「币种」列末尾**（两种布局统一）。0.44.0 挂到名称列是个错误：那一格不是 flex，徽章被换行到下一行把整行撑乱。实测真实页面币种列 131px + flex，徽章跟在币名/时长后完整可见。

## 0.44.0 - 2026-08-27

- **表格模式下 fomo 事件按表格行呈现**：不再是两行卡片挤在表格里，而是与 GMGN 同构的列（时间 | 名称 | 币种 | 金额 | 市值），列宽对齐（名称/币种 120px）、行高对齐（44px）。观点正文在表格模式下另起一行显示并带译文。卡片/表格切换时自动按新模式重建。
- **修复表格模式滚动卡顿**。虚拟列表滚动时每帧回收/重建行，MutationObserver 被打成一片，而调度用的是 `setTimeout(0)`——等于每帧跑一次全量扫描（战壕卡/喊单/宣言/特别关注/持仓/fomo 面板/混排），全压在滚动帧上。改为 `requestAnimationFrame` 合帧 + 滚动期间降频到 ~120ms 一次，滚动停下 180ms 内补一次全量扫描保证不漏挂。

## 0.43.0 - 2026-08-27

- **适配追踪流「表格模式」**（此前只适配了卡片模式，切到表格布局后 fomo 卡与 👤 徽章都错位重叠）。结构取自真实浏览器实测：表格模式行高 44px（卡片模式 64.5px）、没有 `TrackerListItem` 构建标记、虚拟化外壳比卡片模式多隔一层。
  - fomo 卡：向上找几层定位虚拟化外壳，不再只看父层——找不到会误判成流式布局、退化去插兄弟节点，正是重叠的直接原因。现按真实行高逐行位移让位。
  - 👤 徽章：表格模式挂到较宽的「名称」列（币种列只有 120px 且 overflow-hidden，塞进去必然盖住币名）。
  - **通用代币链接不再扫进追踪流**：表格行本身就是 `<a href="/token/...">`，此前会被当成普通代币链接、在行尾再挂一个徽章压住币名——这是用户截图里重叠的元凶。
- **修复追踪面板上 👤 徽章从不出现**：`currentChain()` 只认 `/bsc/token/...` 路径段，而追踪流页是 `/?chain=bsc`，取不到链 → 标注持仓不加载。现同时支持 query 参数。
- **修复持仓暴涨提醒失效**：① 持仓行只认构建期 `data-sentry-component`（部分用户页面没有该标记）→ 持仓清单恒空 → 轮询直接跳过、永不提醒；现补 fiber 兜底识别。② 价格接口用空 `device_id`/`app_ver` 的旧参数 + 剥登录态，与此前 👤 徽章栽过的 Cloudflare 坑同类；现对齐页面自身参数并带登录态。

## 0.42.3 - 2026-08-26

- **修复一键升级失败**（提示"安装包含未授权文件 fomo-early.js"）。升级器用的是精确文件名白名单，v0.42.1 新增的 fomo-early.js 不在其中，导致升级被拒。改为按扩展名放行（.js/.json/.css/.html/.png…）+ 核心文件完整性校验，以后新增文件不再触发此问题；注入 exe/dll 等可执行文件仍被拒（self-test 覆盖）。
  - ⚠️ **已装旧版的用户这一次需要重新下载安装器 exe 装一遍**（配置与名单都在浏览器本地，不会丢）。因为升级器本身不随一键升级更新，旧升级器的白名单改不了。装过这一版之后，以后的一键升级都正常。
- **标注人物 👤 徽章不再遮挡币名**：GMGN 改版后若追踪卡的币名格 testid 变了、定位不到，徽章此前会 fallback 挂到整张卡片上、乱位盖住币名。改为定位不到就不显示（宁可不显示也不遮挡），并顺带清掉可能的残留。

## 0.42.2 - 2026-08-26

- **修复代币详情页 Flap 徽章显示不全**（被标题行截断）。标题那一行是横向 flex（名称+价格+市值），徽章塞进去会被 ellipsis 裁掉。改为越过横向 flex 行、在其外独立起一整行显示，可换行、完整可见。

## 0.42.1 - 2026-08-26

- **修复"fomo 登录态仍会过期"**（症状：面板提示自动续期失败、会话被 privy 作废）。根因不是续期间隔太短，而是分叉：插件续期会轮换 refresh，但写回只能在 fomo.family 标签开着时做——页面关着期间轮换过的链，页面一打开 privy SDK 先用旧 refresh 去续，触发复用检测作废整条会话，插件连坐。两手修复：
  - 新增 `fomo-early.js`（document_start 抢跑）：页面加载最早时刻把插件的最新令牌写进 localStorage，SDK 启动读到的就是新链，分叉不再发生。
  - 保活轮换降频：40 分钟阈值改为 10 分钟闹钟 + 剩余 <12 分钟才续（每小时恰好轮换一次，分叉窗口减半）。
  - 顺带：页面令牌与插件完全一致时不再重复写存储。

## 0.42.0 - 2026-08-26

- **默认标注名单扩到 6 人**：阿峰拆为四个号（大号1 `0xbf00…d4903`、大号2 `0xbd28…a368a`、小号1 `0x92de…59400`、小号2 `0xb9c9…fcc06`）+ 奶牛 + 枯坐。服务器采集名单同步（实测大号2 28 / 小号1 89 / 小号2 82 条持仓已进产物）。保存过设置的老用户自动一次性合并（缺的默认地址补进、旧名"阿峰"改"阿峰大号1"、自己改过的备注不动）。
- **自定义标注人物自动上报服务器**：你在设置里加的人（服务器名单外），后台自动上报进服务器全量采集名单（`POST /api/marked-watch`，共享口令 + 地址校验 + 30 人上限 + 幂等去重）；≤3 分钟后该人也有完整持仓，期间页面直拉兜底无缝衔接。

## 0.41.1 - 2026-08-26

- **fomo 登录态主动保活**。此前续期是被动的（用到 fomo 接口才续），几天不开 fomo 面板 refresh 链会闲置过期。新增 30 分钟保活闹钟：令牌剩余寿命 <40 分钟即自动用 refresh 续上（chrome.alarms 会唤醒后台），续期成功照旧写回开着的 fomo.family 页。效果：登录一次 fomo.family 之后，只要浏览器还开着插件，登录态就一直续着。

## 0.41.0 - 2026-08-26

- **标注人物持仓改为服务器全量采集**（解决"每人前 50 条不够"）。985monitor 服务器每 3 分钟用 GMGN 官方 API（gmgn-cli，带签名）翻页拉完整持仓发布成静态 JSON；实测覆盖：奶牛 455 / 阿峰 435 / 枯坐 195 条（旧口径只有前 50）。
  - 插件端优先读服务器产物（120s 控频 + ETag + 退避），固定名单的人不再各自打 GMGN 接口；你在设置里自己加的人（服务器名单外）仍走原来的页面直拉作为回退。
  - 徽章判定加 $30 阈值：灰尘/空投仓不算"持有"，避免满屏误标；悬停人名旁带持仓金额（如 奶牛($3.8K)）。
  - 名字以插件本地备注优先。
- 服务器侧新增 marked-holdings.service：限速纪律（页间 0.4s/人际 2s，429 即弃轮保留上一份产物）、原子写发布。

## 0.40.2 - 2026-08-26

- **标注人物持仓徽章：请求量加固**。请求数只与名单人数挂钩（默认 3 人 × 每链 2 分钟一轮 ≈ 1.5 次/分钟），徽章渲染是纯本地查表零网络——本就与卡片数量无关；本版再补两处：持仓缓存改为按链各自保存（bsc↔sol 来回切页不再重拉），名单里人与人的请求之间垫 250ms 最小间隔（自定义大名单也不会突发打接口）。

## 0.40.1 - 2026-08-26

- **修复「标注人物持仓徽章」失效**（👤N 徽章一直不出现）。根因实测：GMGN 的 `wallet_holdings` 接口现在被 Cloudflare 盯防，不带其前端那串客户端参数（device_id/client_id/app_ver…）直接 403 拦截页，且原实现 `credentials:'omit'` 把登录态也剥了（接口需登录，未登录 401）。修复：从本页已发过的请求里原样抄一份参数（resource timing），带登录态请求；未登录或参数未就绪时静默跳过本轮。

## 0.40.0 - 2026-08-26

- **fomo 推送接入 985monitor SSE 实时通道，延迟降到秒级**。检查确认服务端链路已就绪（采集器 publish 即 `_sse_push` 广播、`event: fomo` 帧实测可达）；插件后台以 fetch 流订阅 `/api/events-stream`（MV3 无 EventSource），只消费 fomo 帧：瘦身入缓存（按 key 去重）→ 即时通知 GMGN 标签页 → 前台立即取缓存重摆（命中控频内 stale 分支，零额外 HTTP）。
- 可靠性：坏帧不断流、分帧传输正确拼接、断线指数退避重连（5s 起、上限 2 分钟）；SW 被挂起后由前台 18 秒轮询唤醒重连——15/18s 轮询保留为 SSE 断档兜底。

## 0.39.2 - 2026-08-26

- **fomo 推送延迟砍半以上**：延迟链路实测——985 采集端是实时 WS + 5 秒落盘节流（很快），大头在插件轮询间隔（40~45s）。后台控频 40s→15s、前台触发 45s→18s；未变更时走 ETag 304 条件请求，只有头没有 800KB 全量，成本可忽略。端到端延迟从平均 ~27s / 最坏 ~50s 降到平均 ~14s / 最坏 ~23s。

## 0.39.1 - 2026-08-26

- **fomo 卡加链条色竖条**，对齐 GMGN Fusion 模式的原生呈现（5px、盖在卡片左缘）：BSC 黄 `#eab204`、Robinhood 绿 `#9fc700`、Solana 紫 `#7b44f2` 等，默认色表扒自 GMGN 前端；你在 GMGN 里自定义过的链色（`follow_toast_chain_color_v1`）优先跟随。

## 0.39.0 - 2026-08-26

- **观点卡完整显示 + 本地翻译**：混排里的 fomo 观点正文不再单行截断，完整多行显示（观点卡放开 64.5px 定高，位移布局自动适配）；译文走与 fomo 浮窗同一条浏览器本地翻译链路（Chrome/Edge 内置 Translator，含 Edge 卡死检测），原文不动、译文补在下面，随「观点翻译」开关增删。
- **修复"fomo 面板总是要重新登录"**：两处根因——
  - 打开/刷新 fomo.family 页时，网页里的旧令牌会无条件盖掉插件刚续期出来的新令牌，而旧 refresh 已被 privy 轮换作废，下次续期必 401。改为"旧不盖新"（按 JWT 过期时间比较）。
  - 插件续期后 privy 轮换链分叉：网页 localStorage 仍是旧 refresh，网页自己续期时触发 privy 复用检测、连坐作废整个会话。现在续期成功后把新令牌写回开着的 fomo.family 页（保持 privy 的存储格式），两边共用同一条轮换链。
- manifest 新增 fomo.family 主域 host 权限（写回时定位标签页用）。

## 0.38.1 - 2026-08-26

- **fomo 推送默认改为全链显示**（修"事件都没嵌进追踪流"）。原默认"只看当前链"是语义错配：GMGN 追踪侧栏本身就是全链混合的，而 fomo 事件绝大多数不在当前页面链上（实测某 30 分钟窗口 88 条事件里 BNB 链只有 3 条，其余 Solana 51/Robinhood 20/Base 12 全被过滤）。"只看当前链"仍可在设置里手动打开。

## 0.38.0 - 2026-08-26

- **fomo 混排卡换皮成 GMGN 原生卡样式**：两行结构（行1 头像/名字/动作/时间，行2 金额/币 logo/币名/MC:$xx）、同高 64.5px、透明底、去彩边彩底；来源仅以 [fomo] 小标区分。动作词按方向着色（买入绿/卖出红/换仓黄/观点紫），观点正文单行显示、悬停看全文。事件带上代币 logo。
- **fomo 浮窗支持 Robinhood 链**：`/robinhood/token/…` 页出现 fomo 入口与持仓者/观点面板（fomo 数据源 networkId 4663，实测 193 条事件在源）；顺带支持 monad（143）。fomo 不覆盖的链（hyperevm 等）仍不出入口。
- 混排卡点击/当前链识别扩到 GMGN 全链枚举（robinhood/monad/megaeth/hyperevm/xlayer/arc/stable/arbitrum），robinhood 链的混排卡现在也走站内无刷新跳转。

## 0.37.2 - 2026-08-25

- **fomo 混排卡点击改走 GMGN 站内路由**：和点原生代币卡一致，当前页无刷新切换（page-bridge 代调 `window.next.router.push`，实测 Next 16 Pages Router 可用），不再整页重载。bridge 不可用时自动回退普通跳转。
- 修复兜底判定时序：`dispatchEvent` 是同步的、路由在派发内就已改掉 pathname，基准若在派发后取会导致"刚跳完又被整页重载一遍"；基准改在派发前取，且已在目标代币页时不重复跳。

## 0.37.1 - 2026-08-25

- **修复 fomo 推送混排在现网追踪流上重叠**。实测（用户 DOM 诊断）GMGN 追踪流不是动态量高的 virtuoso，而是固定行高公式布局：行包装 `position:absolute; top=index×64.5px; height:64.5px`，行推不动——0.37.0 的"塞进行壳借 ResizeObserver 重排"在这种布局下物理不成立，卡片被后一行盖住。
  - 新摆法：不动行的 `top`，给锚行之后的挂载行加 `translateY(累计卡高)` 让位（React 只重写 top，不碰 transform，两套定位互不覆盖），fomo 卡绝对定位塞进腾出的缝里；行卸载/关开关时位移全部回收。
  - 同屏混排限 6 张，防止把「scrollTop→挂载哪些行」的映射拉出 overscan 冗余。
  - 兜底条落点修正：跳过滚动容器外的 0 高壳（`.virtual-list-container`），插到真正占布局的那层，不再叠在列表上。
  - 流式布局的旧摆法保留（GMGN 改版回流式仍可用），两种模式测试各自覆盖。

## 0.37.0 - 2026-08-25

- **985monitor fomo 推送混排进追踪流**：fomo 交易员的买入/卖出/换仓/观点事件按真实时间插进 GMGN 追踪推送里。
  - 摆放不碰 virtuoso 的布局账本：fomo 卡塞进时间紧邻那张追踪卡的专属外壳里（ResizeObserver 视作该行变高，自动重排）；比所有已挂载行都新的事件进列表上方的兜底条；行时间读不到/没有专属外壳时整体降级进兜底条，任何情况都不往列表里插兄弟节点。
  - 过滤沿用 985monitor 网页配置：屏蔽的人、每人的事件类型开关（打开过 985monitor 页面即自动同步，无需登录态）；追踪里屏蔽的币，fomo 推送同样不显示。
  - 设置：总开关 / 只看当前链（默认开）/ 四类事件开关。
  - 取数：后台 40 秒控频 + ETag 条件请求（304 不拉全量）+ 失败指数退避。
  - 点卡片跳对应链的 GMGN 代币页，点名字/头像开 fomo 主页。
- page-bridge 从追踪卡 React 数据里补提事件时间戳（`timestamp`，秒），供混排锚定。

## 0.36.1 - 2026-08-25

- **Flap 税收徽章可点击**，跳转 flap 官方税收详情页（`flap.sh/bnb/<CA>/taxinfo?lang=zh`），新标签页打开；徽章嵌在 GMGN 卡片链接内，点击已拦截冒泡，不会误触发跳去代币页。支持键盘 Enter/空格。

## 0.36.0 - 2026-08-24

- **搜索结果里也显示 Flap 分红徽章**，文案与战壕卡一致（`🪙BNB | 💎→GMEB`）。
- 定位方式：以搜索框的 placeholder（GMGN 自己写的文案，比构建期标记稳）为锚，往上找到含代币链接的那一层作为扫描范围。**刻意不做全站 `a[href*="/token/"]` 扫描**——那会把徽章撒到持仓、喊单等一堆无关的地方；锚点找不到就什么都不做，不退化成全站扫。
- 仍然只对 7777 / 8888 结尾的 Flap 代币发请求，其余一次都不打。

## 0.35.1 - 2026-08-24

- **修复 fomo 统计条里「持仓占比」永远显示不出来**。取总供应量时我把 `flapRpc(null, …)` 当成"会自动挑公共节点"来用，但它只负责打**单个**节点，回退逻辑在调用方——传 `null` 直接 `fetch(null)` 必然失败（`Failed to parse URL from null`），于是占比一直是「—」。现在同样逐个节点回退，并把用户自定义 RPC 一起带上（优先用）。
- 地址前缀的大小写校验放宽（`0X…` 也认）。
- **这个 bug 是测试打桩打出来的**：统计条的用例里把 `token-supply` 整个 mock 掉了，真实后台处理器从没被跑过。现补 `verify-supply` 直连真实 BSC 节点跑真处理器——覆盖真实取数、人类可读口径、缓存命中、普通 ERC20、非 BSC 链、非法地址、以及自定义 RPC 不通时回退公共节点。

## 0.35.0 - 2026-08-24

- **fomo 浮窗顶部新增数据统计条**，两栏四项：
  - **Fomo 持有人数** —— 该代币在 fomo 上的持有人总数；副行是 thesis 条数
  - **Fomo 持仓占比** —— fomo 持仓者合计持有量占总供应量的比例；副行是合计美元与「已加载/总数」
- 占比标 **≥**：fomo 只返回前 N 名持仓者，所以这是**下界**而非精确值；已加载数等于总数时不再标 ≥。
- 总供应量直读链上（`totalSupply()` / `decimals()`，按地址长缓存），不依赖任何第三方服务；目前只支持 BSC，其它链取不到时占比显示「—」，其余三项照常。

## 0.34.1 - 2026-08-24

- **追踪流不再显示税收徽章**。那一行本来就密（钱包 + 动作 + 金额 + 币名 + 市值 + 时间），再塞一块只会更挤。要看税收去战壕卡或代币页。
- 符号规范化与技术瓜对齐：去标点、`WBNB` 归一为 `BNB`、拉丁最多 4 字、中文最多 6 字（此前一律截 8 字）。

## 0.34.0 - 2026-08-24

- **徽章文案改为与技术瓜 FlapFeeInfo 逐字一致**。此前是我按自己的理解拼的（`💎GMEB 100%`），这次把它的实现扒出来照做，格式是：

  `🪙底池币 | 各去向（按占比降序）`

  每段是 emoji + 占比，**满 100% 时只留 emoji**（`💎` 而不是 `💎100%`）；**占比最大的那一段**额外带 `→收款资产`——分红指向分红币、金库 / 加池指向底池币、销毁不带。例：`🪙GMEB | 💎→GMEB`、`🪙BNB | 💎60%→XYZ💧40%`、`🪙BNB | 🔥80%💧20%`。
- 占比带小数时按一位小数显示并去掉多余的 `.0`（`97.5%`），与其保持一致。

## 0.33.1 - 2026-08-24

- **Flap 徽章改为在币名下面单独占一行**。此前接管原生 `Tax x%` 的位置，但币名那一行本来就挤（名称 + 税标 + 成交额 + 市值，且带 `overflow:hidden`），徽章挤进去会被裁掉、只露出半截。现在在那一行下面新起一行专门放它，完整显示不会被截断。
- 原生 `Tax x%` 仍然藏起（下面那行信息更全），链上数据读不到时照旧把它还回去。

## 0.33.0 - 2026-08-24

- **Flap 徽章改为直接显示「分红给的是哪个币」**，对齐 tech-melon 的呈现方式：例如 `💎GMEB 100%` —— 图标是税收去向，后面是分红资产的**币名**与占比，不用悬停就能看到。加池子模式显示币对（`💧HODL/GMEB 30%`），销毁 / 金库模式显示对应名目与占比。
- 为此把徽章要用的币名一次收齐：代币自身、底池对手币（计价币）、分红资产三个 `symbol()` 批量读取，按地址长缓存——多个币常共用同一分红资产，命中就不再请求。
- **战壕卡上接管 GMGN 原生的 `Tax x%` 标签**：藏起原生的，把信息更全的徽章插在它原位，不额外占地方。按文案定位，不依赖构建期标记；读不到链上数据时会把原生标签还回去，不会两头落空。
- 悬停详情补上币名：分红资产写成「分红资产 GMEB 0x46cE…b15C」，底池写成「底池 HODL/GMEB 0x8D50…6941」。

## 0.32.0 - 2026-08-24

- 新增 **Flap 税收徽章**：7777 / 8888 结尾的 Flap 代币旁显示总税率，悬停展开完整税收分配。按税收去向着色分类——💎持币分红 / 💧加池子 / 🔥销毁通缩 / 🎁金库营销 / 混合。战壕卡、追踪推送、代币页都会标。
- **数据由用户自己的浏览器直读链上**，不经任何第三方服务：代币合约 `getPoolStateData()` / `taxRate()` 给出实时买卖税与状态，`taxProcessor()` 指向的处理器 `feeConfigV3()` 给出各收款方 bps 与分红资产。契约取自链上已验证源码（FlapTaxTokenV3 / TaxProcessorUniV2），批量 `eth_call` 完成，结果缓存 60 秒。
- 默认走内置公共 BSC 节点（三个，自动回退）；配置页可填**自己的 RPC**，填了优先。自定义 RPC 走**可选权限**，保存时当场申请该域名授权——不会为此索取全站访问权。
- 非 Flap 代币的 `eth_call` 会 revert，据此直接判定并缓存，不再反复重试其它节点。
- 新增的两处扫描各自兜住异常，避免它们出错时把同一轮里的 ☆ / 高亮一起带塌。

## 0.31.0 - 2026-08-21

- **翻译适配 Edge**。实测 Edge 151 同样暴露 `Translator` / `LanguageDetector`（接口与 Chrome 一致），但 `availability()` 返回 `downloadable` 后 **`create()` 可能一直挂着**——不返回、不报错、也不发任何 `downloadprogress`。此前没有超时，「译」按钮会永远停在待点状态。
- 现在用**有没有收到下载进度**来区分「真在下载」和「悄悄卡住」：收到进度就耐心等，并在按钮上显示百分比；15 秒内一个进度都没有则判定卡住。
- 提示按浏览器分开写：Edge 卡住时说明多为本机翻译模型未启用/未下载、可在 Edge 设置里检查后点按钮重试；Chrome 走原有文案。**不会再谎称「当前浏览器不支持」**——接口明明有响应。
- 卡住后再点「译」即重试（清掉缓存的翻译器重新创建）。

## 0.30.0 - 2026-08-21

- **战壕的重点 Dev 高亮与 Dev 战绩同样修复**。这是同一个根因的第三处：卡片此前只认 `data-sentry-source-file="TokenItem.tsx"`，而这类构建期标记在部分用户页面上一个都没有。现改为优先用 GMGN 自己写的 `data-testid="trench-token-card"`，旧标记保留兼容。
- 相应放开两处会连带失效的护栏：`PumpSubX` 祖先判断（用于限定"这张卡在战壕三列里"）在卡片自带 testid 时不再要求；高亮样式也不再要求祖先带 sentry 标记，只认插件自己打的 `data-gdh-highlighted`。
- 追踪卡的 ★ 定位样式同样补上不依赖 sentry 标记的写法。

> 已知仍依赖构建期标记的功能：喊单黑名单、宣言弹窗/列表、价格提醒醒目化、持仓暴涨提醒（读持仓面板）。GMGN 当前构建里这几处**没有提供任何 `data-testid`**，缺少可靠锚点，需要拿到实际页面结构后再改，暂未处理。

## 0.29.3 - 2026-08-21

- **钱包列表的特别关注高亮与 ☆ 同样修复**。它此前只靠 `data-sentry-component="WalletTable"` 圈定范围，和 0.29.2 修掉的追踪卡是同一个毛病——那类构建期标记在部分用户页面上一个都没有。
- 改为以 GMGN 自己的标签栏 testid（`follow-tracking-wallet-tab` / `follow-tracking-tab`）为锚，往上找到含钱包地址链接的那一层作为扫描范围。既不依赖构建期标记，也不会扩散到代币页持有者表等别处的地址链接。

## 0.29.2 - 2026-08-20

- **修复部分用户追踪流里高亮/☆/🚫/👤 全都不出现**。插件此前只靠 `data-sentry-component="TrackerListItem"` 找追踪卡，而那是构建工具打的标记——实测有用户页面上**一个都没有**（同一文档里插件的 fomo 浮窗正常工作，说明插件已注入），于是整块功能挂不上去。
- 改为**同时**用 GMGN 自己写的 testid 反查：从 `follow-tracking-row-symbol`（币名行）往上找到同时包含 `follow-tracking-row-maker`（钱包行）的那一层，即为卡片本身。两条路取并集，谁在用谁。
- 已核对 GMGN 当前线上构建（buildId 已更新）：`TrackerListItem` 与这两个 testid 都仍然存在，所以不是 GMGN 删了标记，而是不同布局/构建下带不带它并不一定。

## 0.29.1 - 2026-08-20

- **修复特别关注高亮、☆ 与 🚫 在追踪流里全部失效**。GMGN 改版后，追踪推送卡里的钱包名不再保证是 `<a href="/address/0x...">`（现由组件渲染，地址只在卡片数据里）。插件此前只认那个链接来取钱包地址，取不到就整段跳过——于是高亮、特别关注 ☆、屏蔽币 🚫 会一起消失，而 fomo 浮窗因走另一条路径仍正常，看起来像"只有高亮坏了"。
- 改为从卡片自身的数据里读钱包地址（`maker`）与昵称（`nick_name`），链接仍优先、读不到才兜底。字段名取自 GMGN 自己的 `TrackerListItem`，不是猜的。

## 0.29.0 - 2026-08-19

- 新增**标注人物持仓徽章**：搜索结果与追踪推送里，代币旁显示 `👤N` —— 有几个被标注的人正持有这个币，悬停看具体是谁。默认三人（奶牛 / 阿峰 / 枯坐），配置页可增删。
- 实现上是**反过来查**：不去逐个代币问"持有人里有谁"（那是几十上百个请求），而是查这几个被标注的钱包各自持有哪些币（人数个请求，缓存 2 分钟），再给命中的卡片打徽章。接口契约取自 GMGN 自己的取数代码：`GET /api/v1/wallet_holdings/{chain}/{address}`（eth 链地址需小写）。
- **再次修复屏蔽后追踪流留空白**：0.26.0 只折叠 `[data-index]` 那一层，而 GMGN 在卡片外究竟套了几层壳并不确定。改为从卡片往上逐层折叠——只要某一层「只包着这一张追踪卡」就说明是它的专属外壳，一并折叠；遇到含多张卡的层（列表容器）即停手。不再依赖对层数的假设。

## 0.28.0 - 2026-08-19

- **改名：985gmgn助手 → better gmgn**。扩展名称、图标提示、配置页标题、安装器窗口与 README 全部更新。
- **扩展 ID 没有变**（`manifest.key` 未动），所以升级后你的重点 Dev 名单、特别关注、屏蔽名单、所有配置都还在，不需要重新设置。
- 安装目录、Native Messaging 主机名、仓库名与产物文件名**刻意保持不变**：改了会让已有安装变成孤儿、或让旧更新器找不到升级包。
- 更新器的名称校验改为**同时接受新旧两个名字**，避免跨版本升级时互相拒绝。
- ⚠️ **已装 0.27.x 及更早版本的用户，「一键升级」这一次会失败**（报「插件名称校验失败」）——他们机器上那个更新器里写死的是旧名字。**手动下载并运行一次 v0.28.0 的安装器即可**，之后的升级恢复正常。

## 0.27.1 - 2026-08-19

正式发布前的引导补齐，功能逻辑无改动。

- **README 重写**：此前还停留在最早的「高亮 Dev + 黑名单」，0.12 之后的十几个功能一个都没写。现按 战壕/Dev、喊单/宣言、钱包追踪、提醒、fomo 整合 分组，补上安装步骤、隐私说明与自动更新说明。
- **配置页新增「怎么用」**：只写光看开关发现不了的四件事——追踪流长按 🚫 屏蔽币、悬停 ☆ 与 ★N 管理入口、fomo 浮窗位置与需要 fomo 登录态、持仓暴涨要先打开一次持仓面板。附完整说明链接。
- 顺带修正配置项描述与实际行为不一致之处（fomo 持仓者一项已改为标注链上名次）。

## 0.27.0 - 2026-08-19

- **改变做法：不再往 GMGN 持有者表里插行**。那张表是虚拟化的，**每一行各自被包在一个绝对定位的外壳里**，往里插必然跟它抢布局——0.26.x 插进去的行其实被塞进了某一行的壳内，压在里面看不见。
- 改为在 **fomo 浮窗的「持仓者」里标注「链上#N」**：按持仓量算出这个人在 GMGN 持有者表里排第几，前十名金色高亮。完全不碰 GMGN 的表格，滚动、排序、切标签都不会被冲掉。
- 只按**已加载的行**估算：那张表虚拟化后一次只渲染十几行，插件会随滚动把见到的持仓量并进集合，覆盖越多排名越准。比已加载的最小持仓还少时显示 `#N+` 而不是编一个名次，悬停有说明。
- 配置项文案随之改为「fomo 持仓者标注链上名次」。

## 0.26.1 - 2026-08-19

- **修复 fomo 持仓者根本没并进「持有者」列表**：0.26.0 对着 `HolderItemView`（分包 `6042-*.js`）写的，但代币页那张「持有者 221」表格根本不是这个组件——那是另一处的紧凑挂件。实测线上 DOM 后改为真实行选择器 `[data-testid="token-detail-holders-row"]`，字段取其 React fiber 的 `{address, balance, amount_percentage, usd_value}`；行里没有总量，改用「持币数量 ÷ 小数占比」反推。
- **屏蔽按钮改为长按一秒才生效**：它就在币名旁边，单击太容易误触。按住时有放大+发光的进度反馈，中途松手即取消。解除屏蔽是无害操作，仍是单击。
- 修复管理面板里「屏蔽的币」一栏被挤出可视区：模态框是 `overflow:hidden` 的 flex 列，这一栏此前会被上面的钱包列表顶掉。现固定不收缩并有自己的滚动区。
- 「屏蔽的币」列表加上变更防抖：此前每次扫描（每秒至少一次）都整个重建，真实鼠标点击要求 mousedown 与 mouseup 落在同一元素上，中途一重建 click 事件就不会产生。

## 0.26.0 - 2026-08-19

- 新增 **fomo 持仓者并入 GMGN 代币页「持有者」列表**（默认开，可在配置页关）：按持币数量插到对应位置，显示占总量比例与美元市值。GMGN 行的 `balance` 与 fomo 的 `humanAmount` 都是持币数量，可直接比大小。字段名取自双方前端源码（GMGN `HolderItemView`，分包 `6042-*.js`；fomo 代币页分包 `token-*.js`）。
- fomo 持仓者**没有链上钱包地址**（仓位在 fomo 自己的钱包里），无法与链上持有者去重，因此插入的行一律带 `fomo` 标记与紫色左边条，不会与真实链上持有者混淆。
- **修复屏蔽某个币后追踪流留下空档**：追踪流是 react-virtuoso，靠 ResizeObserver 量每项高度，而它会忽略 `display:none` 的测量结果（避免闪烁），被隐藏的项仍占着位置。改为把外层量高的壳折叠成 0 高度，Virtuoso 才会重新量并收起空位。
- 追踪面板的 **★ 管理面板新增「屏蔽的币」一栏**：列出已屏蔽的币（币名 + 地址缩写），点「恢复」即可解除，不必再去追踪流里找那条推送。
- fomo 持仓者取数的空结果只缓存 15 秒（此前统一 60 秒）：首拉可能赶上 fomo 登录态还没就绪，不该让用户干等一分钟。

## 0.25.1 - 2026-08-19

- 修复 0.25.0 的屏蔽按钮**看不到**：0.25.0 靠"卡片 href 里含代币地址"来识别追踪卡，若 href 不是代币页则整个按钮不会生成。这次从 GMGN 前端源码里读出了真实契约，不再依赖 href。
- 代币地址与币名改为按 GMGN 自己的字段名读取（`TrackerListItem.tsx`，分包 `2085-*.js`）：`base_address` / `base_symbol` 是被交易的币，`quote_*` 是计价币，另有 `token_address` 与 `chain`。href 仅作兜底。
- 按钮位置改挂 GMGN 给币名那一行的稳定标记 `[data-testid="follow-tracking-row-symbol"]`，不再靠"文本恰好等于币名"去猜；两者都不在时才退回动作文案（建仓/加仓…）后面。

## 0.25.0 - 2026-08-18

- 钱包追踪流新增**一键屏蔽某个币**：鼠标移到推送卡上，币名后面出现 🚫，点一下这个币在追踪里就不再刷了。平时按钮完全透明不占眼，只在悬停时显形。
- 按**代币地址**屏蔽而不是按币名（同名币不会连坐），该币的全部推送一起隐藏，多标签页即时同步；再点一次 🔔 即恢复，并有 toast 提示去哪恢复。
- 按钮位置不靠猜类名：`page-bridge.js` 从追踪卡的 React fiber 读代币信息，并用卡片 `href` 里的地址**交叉校验**，一致才采用其 symbol；再用「渲染文本恰好等于 symbol 的叶子节点」定位币名并插在其后。读不到时退回动作文案（建仓/加仓…）后面，功能不受影响。
- 点按钮不会误触卡片跳转（阻止冒泡 + 拦下锚点默认行为）。

## 0.24.0 - 2026-08-18

- 持仓暴涨播报的**同一代币再次提醒间隔改为可配置**（配置页新增下拉：5 分钟 / 15 分钟 / 30 分钟 / 1 小时 / 3 小时 / 6 小时 / 24 小时），默认仍是 1 小时。此前这个间隔写死在代码里（`HOLDING_COOLDOWN_MS`），只能接受 1 小时。
- 配置改完即时生效，不必刷新页面；填成 0 或非数字时回落默认 1 小时。

## 0.23.2 - 2026-08-18

安装器体验修复。用户反馈"能装上，但点扩展页面只弹个浏览器窗口就没了，加载插件时又找不到目录"。

- **窗口里直接显示插件目录**，可选中、旁边带「复制路径」按钮。以前这个路径在界面上压根没出现过，状态栏却写着"请加载下方目录"——用户只能自己去猜 `%LOCALAPPDATA%` 在哪。
- **安装成功后自动把路径复制到剪贴板**，并在窗口里列出 4 步加载指引（开开发者模式 → 加载已解压的扩展程序 → Ctrl+V 粘贴路径 → 选中 Extension 文件夹）。点「打开插件目录」也会顺手复制。
- **修复「打开扩展程序页面」可能只开出一个空浏览器窗口**：原先用 `UseShellExecute = true` 传 `chrome://extensions`，这类浏览器内部协议会先经 shell 解析，可能落到首页而不是扩展页。改为直接把地址作为命令行参数交给浏览器进程。
- 开场白按状态区分（尚未安装 / 已安装待加载 / 刚复制完路径），不会再在启动时谎称"路径已复制"。
- 插件本体代码无改动。

## 0.23.1 - 2026-08-18

- **修复安装器「安装 / 修复」在插件目录残缺时永久失败**，报错「安装失败：插件缺少 manifest.json」。安装流程会先读旧目录的版本号（只用于备份命名），读不出就直接抛错中止——于是"修复"按钮恰好修不了它本该修的那个状态，而且每次点都同样失败。触发方式很容易：**先点了「打开插件目录」**（这个按钮会创建空目录）再点安装，或上次安装被中断、目录被杀毒清理。现在读不出版本号就按"未知"继续覆盖安装。
- 一键升级路径同样收敛：装坏了读不出版本号时按 `0.0.0` 处理，让线上版本判定为更新并顺带把目录修好，而不是卡在同一句错误上连升级也走不了。
- 安装器状态栏文案改为「检测到不完整安装，点击"安装 / 修复"即可直接覆盖恢复，无需手动删除目录」。
- 插件本体代码无改动，仅安装器修复。

## 0.23.0 - 2026-08-17

- **令牌自动续期**：捕获登录态时连 privy 的 `refresh_token` 一起存下来，访问令牌快过期（剩不到一分钟）或被拒（401）时，后台自动去 `auth.privy.io/api/v1/sessions` 换新的再重试。以前每小时就得回 fomo 页面刷一次，现在正常情况下不用再管。只有 privy 明确作废会话时才清空并引导重新登录。
- **修复令牌被误删**：`fomo.family` 上读不到 privy 键时（未登录、privy 还没水合、切页面的空档都会短暂读空），旧代码会把 `fomoToken` 写成 `null`，把一个还能用的令牌直接擦掉。现在读空就什么都不做。
- **修复过期时整块一直闪**：出错时不会设置"已加载"标记，于是每个扫描周期（1 秒）都重来一遍「清空 → 加载中 → 错误框」。现在失败后压 20 秒冷却，期间只有手动重试、切换代币/标签、或令牌到位才会再打；已经显示的引导也不再被反复重建。
- **引导重做**：从一句干巴巴的报错，改成四步可照做的流程（打开 fomo → 确认已登录 → 进自己主页也就是持仓页 → 切回来自动接上），配醒目的「打开 fomo 并登录」按钮，并明确显示登录态状态：尚未取到 / 有效还剩多久（是否自动续期）/ 已过期多久。
- **令牌到位自动接上**：`chrome.storage.onChanged` 监听到令牌写入后，浮窗自动重新加载，不需要回来点「重试」。顺带修掉一个小问题——`fomoToken` 以前会被塞进 `settings`。

## 0.22.1 - 2026-08-17

- 观点翻译改为**默认开启**，并且**保留原文**：译文不再顶替原文，而是作为独立一行补在原文下方（左侧蓝线、压暗底色，一眼分得清哪句是原文）。关掉时只移除译文行，原文不动。
- 默认开启带来一个新问题：语言包首次下载必须发生在用户手势里，而首屏没有手势。现在会先探 `Translator.availability()`，缺包时不去硬调 `create()`（会被浏览器直接拒绝），而是把「译」按钮变成黄色待点状态并提示"点一下开始下载中文语言包（只需一次）"；这一下点击只用于授权下载，不会顺手把翻译关掉。
- 修复命中翻译缓存时译文不显示：缓存路径是在渲染途中同步执行的，那时整行还没挂进文档，被 `isConnected` 判定挡掉了。

## 0.22.0 - 2026-08-17

- fomo 浮窗「持仓者」新增**7 天盈亏标记**：每行名字后带一个彩色标签，直接看出这人最近一周赚还是亏。分档同时看金额和收益率、取较低的一档——大户小赚不算高手（+$60K 但只 +2% → ⚪），小号翻倍也不算大神（+80% 但只 +$2K → ⚪）。🔥顶级 / 🟢盈利 / ⚪持平 / 🔴亏损 / 💀重亏，悬停看金额、比例与组合规模。
- 取数走 `/v2/userTokens/aggregatedSnapshot`，用同一条快照序列的首末差算 7 天盈亏，一个用户只要一个请求（fomo 自己的悬浮卡要两个）。**只给滚动到可见的行取数**，并发限 4、同一用户 10 分钟内不重复请求，避免被 fomo 限流。
- 新增 **fomo 榜单标注**：名单来自 985monitor 的 24h/7d/30d/总榜 + 聪明钱 Top300 + KOL 名册 + 追踪名单资金档，共 401 人。榜上的人显示 🏆总榜#1 这样的名次（前十高亮），另有 🧠聪明钱、⭐KOL 与 🐳🐬🐟🦐 资金档，悬停看完整来历。持仓者和观点两个标签页都会标。
- 新增**观点翻译**：面板标题栏的「译」按钮，把英文等非中文观点就地翻成中文，中文原文不动。用的是 Chrome 138+ 的内置本地翻译（与 985monitor 同一套 API），**全程在本机跑、不外传**；首次会下载语言包，所以做成按钮而非自动开启。已翻译的段落左侧有蓝线，悬停看原文，关掉即还原。

## 0.21.2 - 2026-08-17

- 彻底修好 fomo 浮窗「持仓者」空列表与「观点」用户名全是"匿名"：0.21.0 硬猜字段名、0.21.1 按语义猜，都是猜。这次从 fomo 前端自己的代码里读出了真实契约，按它写死。
- 补上 fomo 每个请求都带的 `X-Supported-Chains` 头（缺它 `/hodlers/top` 返回空，这是「持仓者」空列表的直接原因）。
- 修正响应解析：`/hodlers/top` → `responseObject[0].{totalHolders, topHolders}`；`/feed/token*` → `responseObject.items`。
- **fomo 失败时照样返回 HTTP 200**，真实结果在 body 的 `success` / `statusCode` 里。以前不看这两个字段，于是把"登录过期"渲染成"没有数据"。现在会明确显示过期提示并带上 fomo 的原始错误文案。
- 按 fomo 的真实字段与算式渲染：持仓者取 `holder.user.{userHandle, profilePictureLink}`；动态条目的用户信息是**扁平**的 `item.{userHandle, displayName, profilePictureLink}`（两者结构不同，这是"匿名"的根源）。盈亏百分比由 `pnl / costBasis` 算出（fomo 没有现成字段），持有时长由 `averageHoldTimeSeconds` 秒数格式化，均价按小数精度显示，观点正文取 `comment.comment`。

## 0.21.1 - 2026-08-13

- 修复 fomo 浮窗「持仓者」为空、「观点」里用户名全是"匿名"：0.21.0 靠一份硬猜的字段名清单取值，与 fomo 实际返回对不上就全落空。现改为**按语义自适应识别字段**（键名怎么拼都能命中用户名/头像/持仓/盈亏/入场市值/持有时长/观点），并对 `/hodlers/top` 的响应改为递归找出其中的对象数组，不再依赖包装层字段名。
- 浮窗标题栏新增 `{}` 调试按钮：一键显示接口返回的原始首条数据与条数，字段对不上时无需开 DevTools 即可定位。

## 0.21.0 - 2026-08-13

- fomo 浮窗新增「持仓者」标签（默认打开的第一个标签）：列出该代币在 fomo 上的持仓者——交易者、持仓金额、盈亏（金额+百分比，涨绿跌红）、平均入场市值、平均持有时长与观点。
- 接口 `GET /hodlers/top?tokens=[{address,networkId}]`（fomo 内部拼写是 hodlers，故此前按 holders 搜不到）；响应做了 hodlers/holders/items/positions 多字段兼容。

## 0.20.2 - 2026-08-13

- fomo 浮窗优先**直接复用浏览器里的 fomo 登录态（cookie）**：以前没捕获到 Bearer 令牌时直接放弃、压根没发请求；现在先带 cookie 试一次，cookie 认就无需任何额外操作，只有 cookie 也被拒（401）才提示去 fomo.family 抓一次令牌。
- 补上 background 取数逻辑的自动化测试（此前无覆盖）：cookie-only 请求、Bearer 携带、401/403/网络失败分流、请求参数与 20 秒缓存。

## 0.20.1 - 2026-08-13

- 修复 fomo 浮窗"一直卡在需要登录"看不出原因的问题：原先把「没拿到令牌 / 令牌过期(401) / 被 Cloudflare 风控(403) / 网络失败」全折叠成同一句提示。现在分别显示具体原因、HTTP 状态码、以及"令牌：已获取/未获取（多久前）"，并带「打开 fomo.family」与「重试」按钮。
- fomo 请求改为 `credentials: 'include'` 并补 `Accept: application/json`，更接近正常浏览器请求，降低被 Cloudflare 拦截的概率。

## 0.20.0 - 2026-08-13

- 新增"fomo 浮窗"（默认开）：GMGN 代币页右下角出现 `fomo` 按钮，点开即可在 GMGN 内查看该代币在 fomo.family 上的**观点（Thesis）**与**交易**，含头像、用户名、盈亏（涨绿跌红）、金额与时间；面板可拖动并记住位置，30 秒自动刷新，↗ 一键跳 fomo 对应页面。
- 需要先在 fomo.family 登录并打开一次（插件只在 fomo.family 上读取你自己的登录令牌，存本地、仅发给 fomo 官方 API）；未登录时面板给出引导链接。
- 接口：`/feed/token/thesis` 与 `/feed/token`（`threshold=0`、按 networkId 区分链），请求在后台 service worker 发起以避开页面 CORS/CSP。
- 兼容性：为保证老用户"一键升级"可用（更新器对安装包文件有白名单校验），fomo 令牌捕获逻辑并入既有 `content.js`，未新增任何扩展文件。

## 0.19.0 - 2026-08-13

- GMGN「添加钱包」弹窗内新增"同时特别关注"设置行：勾选后可直接选高亮颜色（7 预设 / 炫彩 / 自定义取色）与"📌 推送置顶"，点"添加钱包"时一并写入特别关注名单（备注取弹窗里填的钱包名称）；记住上次选择，默认不勾选，不影响 GMGN 原有添加流程。
- 该行用 placeholder（钱包地址/钱包名称）与按钮文案（添加钱包）定位，不依赖 GMGN 类名与组件标记。
- 持仓暴涨提醒的同代币冷却由 10 分钟延长为 **1 小时**。

## 0.18.0 - 2026-08-13

- 新增"持仓暴涨提醒"（默认开，阈值默认 5 分钟 +20%）：持仓代币短时急涨时在画面正中弹 🚀 大卡片，点击直达代币页。
- 持仓清单从持仓面板行读取并缓存（含 `chain`），**面板关掉后仍持续监控**；切链再开一次面板即可把该链持仓也纳入，之后多链同时监控。
- 行情走 GMGN 公开接口 `mutil_window_token_info`（`credentials: omit`，不带任何登录凭据），30 秒一轮按链分批查询；同一代币 10 分钟冷却，标签页不可见时不轮询。
- 配置页新增开关与阈值选择（+10%/+20%/+30%/+50%/+100%）。

## 0.17.0 - 2026-08-13

- 新增"价格提醒醒目化"（默认开）：GMGN 的到价/市值提醒原本只弹一条很窄的小 toast（`RemindToast`），现接管为画面正中的大卡片——铃铛摇动、脉冲辉光、涨绿跌红、大字代币名与目标值，点击直达代币页。
- 15 秒自动消失、悬停暂停、最多叠 3 张、× 手动关闭；原生小 toast 同步收起避免重复；关闭开关立即恢复 GMGN 原样。

## 0.16.0 - 2026-08-11

- 钱包追踪面板头部新增"★N"特别关注管理入口：面板内查看全部特别关注（颜色/置顶/备注/地址），可改色、切换置顶、移除，也可手动输入地址+备注直接添加。
- 钱包地址页（关注/添加钱包处）"已追踪"按钮旁常显 ☆，添加钱包时可同步打星。
- 调色浮层升级：7 预设色 + **炫彩**（背景/左边条/★ 彩虹流动动画）+ **自定义取色器**，并带"📌 新推送置顶 10 秒"开关。
- 置顶推送：被置顶钱包在追踪流出现新事件时，卡片副本置顶在面板顶部 10 秒（最多叠 3 张，点击直达代币页，可手动关闭）。

## 0.15.1 - 2026-08-11

- 修复"隐藏闪电交易按钮"不生效：实际按钮宿主是 frontrun 插进 GMGN DOM 的 portal 元素 `[data-frontrun-portal="instant-trade"]`（0.15.0 只匹配了 frontrun-csui 容器）；且其 shadow 内有 `:host{display:...!important}` 防隐藏加固（压过外部 display:none），改用 visibility+尺寸归零方案隐藏（真实页面实测生效）。含快捷键 tooltip 变体；frontrun 的 cta 快速买入按钮与其他挂件不受影响。
- 应使用反馈将该开关改为**默认开启**（可在配置页关闭）。

## 0.15.0 - 2026-08-11

- 新增"隐藏闪电交易按钮"开关（默认关）：隐藏 frontrun 插件注入 GMGN 页面的"闪电交易"挂件（识别其 `frontrun-csui` shadow 容器内文案，仅隐藏闪电交易那个挂件，"快速买入"等其余 frontrun 功能不受影响）。

## 0.14.0 - 2026-08-10

- 特别关注支持每个钱包单独设置高亮颜色：★ 旁悬停显示当前色小色块，点击弹出 7 个预设色一次选（金/红/绿/蓝/紫/粉/青），卡片背景、左边条、★ 同步换色。
- 颜色随名单存入 `specialWallets`，多标签页同步；旧数据无颜色字段自动落默认金色。

## 0.13.0 - 2026-08-10

- 新宣言弹窗改到**画面正中**并加大加醒目：样式对齐 GMGN 官方宣言悬浮卡（头像、名字、认证√、倍数、相对时间、完整正文、代币+金额行），金色描边+辉光+弹出动画。
- 宣言列表标签页的每一行复用同一套卡片样式（头像等按列表密度缩小）。
- page-bridge 补提宣言的头像/认证/倍数/时间字段。

## 0.12.1 - 2026-08-10

- 追踪事件卡的 ⭐ 特别关注按钮改挂在"建仓/加仓/减仓/清仓"动作文案右边（原为卡片右侧绝对定位；找不到动作容器时仍走右侧兜底）。
- 钱包列表行的 ⭐ 位置不变（名字后）。

## 0.12.0 - 2026-08-09

- 新增钱包追踪"特别关注"高亮：追踪事件卡片和钱包列表行悬停显示 ⭐ 按钮（平时隐藏），点击加入/取消特别关注；特别关注钱包的事件卡与列表行显示金色左边条 + 金色底色，⭐ 常亮。
- 关注名单持久化 `storage.local.specialWallets`，多标签页即时同步；按钮点击不触发卡片跳转。
- 配置页新增"特别关注高亮"开关（默认开）。

## 0.11.0 - 2026-08-08

- 喊单窗 tab 行新增"宣言"标签：按时间倒序列出当前全部宣言（代币、金额、倍数、宣言人、正文、相对时间），点击行直达代币页（宣言栏同 ulid chip 在场走 SPA 代理点击，否则整页跳转；Ctrl/中键可新标签页打开）。
- 数据来自 GMGN 站内 declaration/global_snapshot 接口；打开时拉取、打开期间每 30 秒自动刷新、15 秒内复用缓存；被屏蔽宣言人的宣言从列表过滤；标签显示当前条数。
- 点 GMGN 自己的"我的/GMGN全部"标签或 × 自动收起；与黑名单弹层互斥。配置页新增"宣言列表标签"开关（默认开）。

## 0.10.1 - 2026-08-08

- 修复新开页面重复弹旧宣言：GMGN 宣言栏先挂空容器、快照数据晚到，基线过早提交导致旧宣言被判为新宣言。基线改为等快照真正到达才提交（空栏走 8 秒兜底）。
- 已见宣言 ulid 持久化到 `storage.local`（上限 500），跨页面、跨标签页不再重复弹；多标签页写入互相合并且不回写，避免循环触发。

## 0.10.0 - 2026-08-08

- 新增“新宣言弹窗”：宣言栏出现新宣言时页面右上角弹卡片（代币、宣言人、正文、金额），点击直达代币页（优先走 GMGN 自身 SPA 路由），12 秒自动消失、悬停暂停、最多叠 3 张。
- 首次加载只登记已有宣言不弹窗；按宣言 ulid 去重，重复挂载不重复弹；被屏蔽宣言人的宣言不弹。
- 配置页新增“新宣言弹窗”开关（默认开）。

## 0.9.0 - 2026-08-08

- Dev 战绩条新增历史最高市值：文案压缩为 `迁X发Y·Z%·峰$MC`，峰值按档位着色（灰 <$100K / 蓝 $100K–1M / 紫 $1M–10M / 金 ≥$10M），悬停显示最高市值币名。
- 数据来自 GMGN 站内 `dev_created_tokens` 接口（同源自动带会话，无需配置）；同 dev 缓存 5 分钟、串行限速 250ms，失败静默隐藏峰段。
- 迁移率显示精简：0% / <0.1% / 1.7% / 33% 风格，去掉 `DEV` 前缀与 0.00% 长尾。

## 0.8.2 - 2026-08-08

- 修复 GMGN 改版（移除 `GlobalCalloutPanel` 标记）后，喊单面板头部“黑名单”管理按钮和列表弹窗消失的问题；改为从喊单 tab 按钮定位面板，旧版结构继续兼容。

## 0.8.1 - 2026-08-07

- 测试 Native Messaging 更新提醒、GitHub Release 检查和一键升级完整链路。

## 0.8.0 - 2026-08-07

- 新增 Windows 单文件安装器与 Native Messaging 本地更新器，同时支持 Chrome 和 Edge。
- 插件启动及每 6 小时检查 GitHub 最新 Release；发现新版后在插件图标显示 `UP` 角标。
- 配置页显示当前版本和最新版本，可查看最新版 GitHub 页面或一键下载安装。
- 更新器仅接受固定 GitHub 仓库，校验 Release SHA256、文件白名单、manifest 名称、版本和固定扩展 ID。
- 升级前自动备份当前插件目录，替换失败时恢复旧版本，并仅保留最近 3 份备份。
- GitHub 标签发布流程新增安装器 EXE、ZIP 与对应 SHA256 文件。

## 0.7.0 - 2026-08-07

- 配置页新增“一键检查升级”入口，调用 Chrome 官方更新检查 API。
- 新增后台更新监听；新版下载完成后立即重载插件并应用更新。
- 新增白名单打包脚本和 GitHub 标签发布工作流。
- 补充 GitHub Release 与 Chrome Web Store 更新边界说明。

## 0.6.0 - 2026-08-07

- 图标改为真实透明背景，并放大青蛙主体以提升 16/32 像素辨识度。
- 配置页新增全部五项用户功能的独立开关。
- 关闭功能后立即清理对应高亮、战绩条、悬停提示、收藏按钮或黑名单过滤效果。

## 0.5.0 - 2026-08-07

- 插件名称统一改为“985gmgn助手”。
- 新增以 GMGN 绿色形象为灵感的立体像素青蛙图标，并接入 16/32/48/128 像素扩展图标。
- Popup 标题区展示新版品牌图标。

## 0.4.1 - 2026-08-07

- 修复喊单卡片“屏蔽”按钮的插入位置，固定显示在喊单人标题行末尾。

## 0.4.0 - 2026-08-07

- 喊单面板头部增加“黑名单”管理按钮和名单数量。
- 每条喊单卡片增加“一键屏蔽”按钮，按喊单人钱包与 X 用户名双重识别。
- 被屏蔽人的喊单卡片及顶部宣言通知同步隐藏。
- 黑名单持久化到 Chrome `storage.local`，支持在管理列表中解除屏蔽。

## 0.3.3 - 2026-08-06

- Dev 战绩标签根据迁移比例连续改变亮度。
- 0% 使用低亮度颜色，迁移比例越高，文字、边框和光晕越亮。

## 0.3.2 - 2026-08-06

- 增加每秒一次的轻量卡片同步兜底，确保 GMGN 延迟刷新的 Dev 数据也能更新到战绩条。
- 定时同步只扫描当前战壕卡片，不重复扫描开发者详情面板。

## 0.3.1 - 2026-08-06

- 修复 GMGN 虚拟列表滚动复用卡片后，Dev 战绩条可能残留上一张卡数据的问题。
- 对战壕内部滚动做 100ms 防抖重扫，避免连续滚动时频繁扫描。

## 0.3.0 - 2026-08-06

- 每张 GMGN BSC 战壕卡片常显 Dev 战绩条。
- 战绩条同时展示 Dev 发币迁移数、Dev 发币总数和 Dev 发币迁移比例。
- 战绩条复用卡片原有指标行，不遮挡右侧快速买入按钮。

## 0.2.1 - 2026-08-06

- 避免开发者面板按钮状态刷新触发重复 DOM 扫描。

## 0.2.0 - 2026-08-06

- 在 GMGN“开发者代币”面板增加“一键收藏 Dev”按钮。
- 收藏地址严格取 GMGN“代币统计”中的开发者地址，不使用 Binance Dev。
- 自动备注为“当前代币名 + dev”，例如 `WinkCZdev`。
- 战壕卡片直接常显备注，悬停仍可查看三项 Dev 统计。

## 0.1.2 - 2026-08-06

- 缺失的 Dev 统计显示为 `--`，不再误报为 0。
- 零迁移比例统一显示为 `0.00%`。
- 取消重点状态时立即关闭对应悬停提示。

## 0.1.1 - 2026-08-06

- 修复 Chrome 后台标签页暂停动画帧时，卡片扫描不会启动的问题。

## 0.1.0 - 2026-08-06

- 首次版本。
- 支持本地配置重点 Dev、战壕卡片高亮和备注标签。
- 支持悬停显示 Dev 发币迁移数、发币总数和迁移比例。
- 无需 API Key，直接读取 GMGN 当前战壕卡片数据。
